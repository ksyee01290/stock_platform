# Development / test dependencies (not required to run the server).
# NOTE: passlib is incompatible with bcrypt >= 4.1, so bcrypt is pinned.
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
PyJWT>=2.8
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
